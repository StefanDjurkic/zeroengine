# Place `jspp.exe` (Windows) or `jspp` (Linux/macOS) here to bundle it
# inside the ZeroEngine desktop installer. The Rust bridge searches
# `<exe_dir>/jspp*` and `<exe_dir>/resources/jspp*` at startup.
